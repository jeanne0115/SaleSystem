using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class LoadTest : MonoBehaviour {

    public Entity_Sale saledate;

	// Use this for initialization
	void Start () {
        //var sale = saledate.param.Where(date => date.id == 1).FirstOrDefault();
       // if(sale != null)
       // {
         //   Debug.Log(sale.name);
        //    Debug.Log(sale.price);
        //    Debug.Log(sale.id);
       // }
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
